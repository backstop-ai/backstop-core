name: backstop/entrypoint-alpha
version: 1.0.0
language: go
archetype: enforcement
description: >
  First of two fixture packs each declaring one toolchain entrypoint, so each is executed
  and reported separately.
engines:
  alpha-test:
    command: go build -o alpha.marker ./...
    input_mode: none
    scope_kind: project-wide
    category: mechanism
    gate_type: test

# Not an artifact

Markdown with NO artifact suffix. It is the load-bearing non-artifact file: a classifier
keying on `.md` rather than on `.spec.md` / `.adr.md` / `.bundle.md` / `.issue.md` /
`.directive.md` would report it, and it must never appear in the deviation set.

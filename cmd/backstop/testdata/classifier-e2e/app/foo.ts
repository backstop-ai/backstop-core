// Sample changed NON-Go source file. The merged classifier (bun source **/*.ts)
// classifies it as measurable source; with no coverage engine it has no record,
// so the gate reds loud rather than vacuous-green (the bundle's keystone hole).
export function foo(): number {
  return 42;
}

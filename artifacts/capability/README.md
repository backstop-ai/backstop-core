# Capability

Defines a discrete capability that an agent or system component provides. Used for agent profiling and tool whitelisting.

## Schema

- `schema.json` — JSON Schema for capability validation

## Versioning

Schemas are versioned in subdirectories (e.g., `v1/`). The root `schema.json` always points to the latest stable version.

# ADR (Architecture Decision Record)

Records a design decision with context, rationale, and consequences.

## Schema

- `schema.json` — JSON Schema for ADR validation
- `template.md` — Markdown template for new ADRs

## Versioning

Schemas are versioned in subdirectories (e.g., `v1/`). The root `schema.json` always points to the latest stable version.

# Coverage exclusions for backstop-core.
#
# FORMAT: <repo-relative-path><TAB><justification>
# Read by the go-toolchain pack's coverage producer, which folds each line into the
# coverage profile so the sandboxed converter can mark the record excluded. A line
# with no justification column is DROPPED, not honoured — the path stays measured.
#
# TRACKED ON PURPOSE. A gitignored declaration would shape the gate's verdict without
# appearing in review.

pkg/packval/sandbox_linux_helper.go	exact exec-erased production inventory: applyRestrictionsAndExec, applyLandlock, and non-empty applySeccomp run inside the re-exec helper whose unix.Exec replacement prevents coverage counters from flushing. Historical evidence remains unchanged at pkg/packval/testdata/sandbox-linux-coverage-profile.txt (run 30386673582). Pure seccompAuditArch and seccompSyscallNumbers moved to measured direct-test ownership in sandbox_linux.go; no pure decision or injected orchestration is excluded.
pkg/packval/sandbox_nonlinux.go	//go:build !linux -- CI runs exclusively on ubuntu-latest (.github/workflows/ci.yml), so this file never compiles into the binary under test there and can structurally never produce a coverage record on CI, regardless of how well it's covered locally on darwin. Surfaced by ISSUE-168 (ci run 32142326172), the first change to this file to ever reach a real CI run.

## FYI ISSUE-092 "Pack Test Phase3 Fixtures Cannot Fail" — slotted into DIR-024 under the standing grant (2026-07-28T02:00Z)
- Classification: clear-fit DIR-024 "Gate/Engine Quality" (the catch-all for gate/engine-quality gaps that aren't contracts-engine, pack-distribution, or traceability themed)
- Action taken: slotted via directive-author. `ISSUE-092` added to DIR-024's `source:`, a new Description item 6 records the root cause and the two measurement traps, Notes carry the sequencing caveats below. Status left `queued`. **BACKLOG.yml untouched.** `artifact validate --directive DIR-024` → ✓ all checks passed.
- **What the issue is**, in one breath: `backstop pack test`'s phase3-fixtures step — the mechanism that enforces the must-falsify convention (every negative fixture must actually trigger its rule) — never runs. `pkg/packval` reads a rule's source file from a YAML key `file:`; every real pack.yml in the fleet declares it as `rule_path:`, which is what the *runtime* gate parser consumes. Two independent manifest models, silently drifted. So `rule.File == ""` for every pack, four guards in `phase3.go` go false, `executor.RunEngine` is never called, zero fixture checks execute, and the phase reports `pass`. Measured by implementer-087: rewriting a negative fixture to delete the violation it exists to catch still yields `phase3-fixtures: pass`, exit 0.
- I verified the root cause independently rather than taking the issue's word: `rule_path`/`RulePath` appears **nowhere** under `pkg/packval/` (grep clean). The drift is real.
- Why DIR-024 and not somewhere else: two of DIR-024's five existing sources already point into `pkg/packval` (ISSUE-020 `sandbox.go`, ISSUE-082 `executor.go`), and ISSUE-075 — already cited there — is the identical failure shape (a smoke fixture that passed for 12 days while proving nothing). No second directive has a plausible charter claim; DIR-027's interest is a dependency, not a fit. That's why I treated it as clear-fit rather than escalating.
- In-flight coverage: **none.** No `PLAN-ISSUE-092` exists. The two live sessions that mention it (both last active 21:58) are the discovering session and the filing session — implementer-087 explicitly worked *around* this defect (invoking semgrep directly on explicit file targets) rather than fixing it, which is why it got filed. No interview needed, none run.
- Needs from Brandon: nothing on the slot — FYI. But the proposal directly below is a real decision.

## PROPOSAL ISSUE-092 outranks its own directive — DIR-027's fleet-CI acceptance criterion is vacuous while it stands (2026-07-28T02:00Z)
- Classification: priority — sequencing, NOT applied. No reorder made.
- **The coupling.** DIR-027 "Pack Fleet Publication & Migration" sits at backlog position 3 (a launch blocker). Its thread 5 and one of its acceptance criteria require every published pack repo to run `backstop pack check` + `backstop pack test` in CI on every push — the publisher-side gate that stops a broken tag from shipping silently. While ISSUE-092 stands, `pack test` returns green for a pack whose rules never fire. Landing DIR-027 thread 5 first buys a fleet-wide CI gate that is **green by construction** across all ten published packs.
- **What I recommend, and it is deliberately not a reorder.** Sequence ISSUE-092 ahead of DIR-027 thread 5's completion, leaving DIR-024 at position 4. Moving DIR-024 up would drag ISSUE-007/075/077/082 — genuinely low-urgency ride-alongs — ahead of launch work, which is worse than the problem it solves. The honest fix is one issue jumping its directive's queue position. I recorded that in DIR-024's Notes as a sequencing note pending your call, not as a decision.
- **Second-order ask.** DIR-024's Notes currently instruct anyone working it top-down to land ISSUE-020 (Linux sandbox) first and not to pick up the others ahead of it. ISSUE-092 is the first source in that directive with a legitimate claim to compete with ISSUE-020 for first pick. I framed that as a founder call in the Notes rather than ruling on it.
- **Cluster note, no action requested yet.** ISSUE-092 is the fourth member of the gate-verdict-honesty family — a verdict surface that reads authoritative and silently isn't. On 2026-07-27T22:59Z I narrowed that cluster 4 → 2 (ISSUE-066 "Gate Test Verification Runs Full Package, Not a Plan's Narrow -run Filter" + ISSUE-067 "go-test Engine Reports Test Failures As an Opaque Crash") and *changed* my recommendation to "append to DIR-024, no new directive." Today it re-grew to four: ISSUE-091 "Gate All Underreports Test File Findings" (filed ~13h ago) and now ISSUE-092, both `risk: critical`, both open, and **all four cited by no directive**. I am not re-litigating the new-directive question on my own initiative — append-to-DIR-024 still works and is cheaper. Flagging only that the case you were weighing has changed shape twice in one day.
- Needs from Brandon: (a) yes/no on sequencing ISSUE-092 ahead of DIR-027 thread 5; (b) if yes, whether it also jumps ISSUE-020 inside DIR-024; (c) optionally, whether ISSUE-091 should be slotted into DIR-024 alongside 092 — it is uncited, same family, and ISSUE-092's own fix notes warn that restoring fixture execution via a directory scan would inherit ISSUE-091's undercount, so the two fixes are technically entangled.

## FYI confidence-pass follow-ups — all 4 applied and verified; the gate-honesty escalation below has MATERIALLY CHANGED (2026-07-27T22:59Z)
- Classification: 4 approved follow-ups executed under the grant as mechanical/evidenced. `artifact validate --all` is GREEN.
- **DIR-026 — done.** `SPEC-056` added to `source:`, closing the traceability hole (it was cited by no directive anywhere). REQ-039 bullet rewritten from "highest-value remaining seed" to DELIVERED, recording what actually landed. Remainder corrected to transactions + parity suite; REQ-041's DEMOTED framing kept. Status stays `active`, and the Notes now flag explicitly that whether DIR-026 is close enough to `done` is a founder call, since ISSUE-083 is post-launch and not-to-be-planned and may not be holding it open on merit. One nuance the author caught and handled rather than steamrolling: SPEC-056 built the check that CATCHES the harness-toolchain pack's manifest/tag drift (`0.1.3` vs tags stopping at `v0.1.1`) but never retagged that pack, so the counterexample was reframed as validated-against rather than deleted. ISSUE-083's material and both guards untouched.
- **DIR-019 — done.** Stale sequencing paragraph retired (079 closed, 081 Gaps 1–2 delivered); ISSUE-085 rewritten past-tense as delivered, keeping the REQ-018-critical-path reasoning and the DIR-026-caused/DIR-019-owned attribution. Next-up re-pointed to ISSUE-080's remainder as the highest-severity open item, ahead of ISSUE-081 Gap 3. Status stays `active`. The author re-verified the `recipes/` claim against the tree before restating it — three subdirs still `.gitkeep`-only, no pack declares a `recipes:` block, so REQ-018 is genuinely unbuilt.
- **ISSUE-070 — CLOSED, and the verification mattered.** I flagged a risk that `43ddeb6` delivered ISSUE-068 and ISSUE-070 together and that `334b441` reverted the 068 half. The author checked instead of assuming: both commits touch `gate.go` and `pack_gate.go`, but the revert removed only the `sharedRunCache`/`run_group` plumbing and never touched the scope-filter call. Fix confirmed live (`activeScope.FilterViolations` at `cmd/backstop/gate.go:842`, wrapper at `pkg/gate/scope.go:229`), all three mandated test files present, 8 tests pass under `-race`.
- **ISSUE-075 — CLOSED, with falsification.** Appropriate given the issue is itself about a test that passed while proving nothing for 12 days. `createSpec` now honors a status override and the smoke test passes `implemented` explicitly. Run uncached with `-count=1` (caching is what hid the original red). The author then reintroduced the exact defect in a scratch copy and confirmed `TestSmoke_GateFailsMissingMandatedTest` FAILS — so the guard is genuinely non-vacuous, not merely green.
- Needs from Brandon: nothing on these four — FYI. But read the changed recommendation in the gate-honesty escalation below before ruling on it.

## FYI ISSUE-090 — slotted into DIR-001; my ID-reservation escalation is now fully closed (2026-07-27T22:58Z)
- Classification: clear-fit DIR-001 (founder-named home, not a PM judgment call)
- Action taken: slotted via directive-author. `ISSUE-090` added to DIR-001's `source:`, a Description bullet cites the fix, and Notes carry a citation paragraph following the ISSUE-087 precedent. Status left `queued`; BACKLOG.yml untouched. `artifact validate --directive DIR-001` → all checks passed.
- What the citation pins: the resolver must reconcile `max(git tags, local disk scan)` rather than tags alone, which also makes the silent `ChainedResolver` fallback non-lossy. It belongs to DIR-001 because its trigger condition is literally this directive's own "add the remote" step — it is sequenced work, not general hardening. The Notes also record that the 2026-07-27 backfill already closed the immediate hazard, so ISSUE-090 must not be read as blocking the remote on its own merits; it is just far cheaper to land before that step than after.
- Correction to my earlier entry below: the ID reuse is CORRECT, not a wart. The old empty ISSUE-090 stub was deleted BEFORE this allocation, and the number now carries a real `backstop/issue/090` reservation tag. I flagged it as a recycled number on incomplete information — disregard that flag.
- This closes both halves of my ID-reservation escalation: backfill executed and verified, resolver fix filed and homed. Nothing further needed from you on it.
- Needs from Brandon: nothing — FYI.

## FYI apply pass — ISSUE-086 split, DIR-003 slot + refresh hold, DIR-025 decoupled (2026-07-27T22:47Z)
- Classification: three founder-ratified applies, all executed via the agent write path
- **ISSUE-086 SPLIT — done.** Narrowed to fact 2 only via issue-author. New title: "The Published Baseline Artifact Is Generated With Zero Packs Installed". File renamed `ISSUE-086-core-ci-hand-baked-and-baseline-packless` → `ISSUE-086-published-baseline-generated-packless` (issue-author's bash was blocked by agent-guard; so was mine — every subagent is blocked from bash file-ops on artifacts, only the top-level session can `git mv`. It landed via `main`; noting the constraint because it will recur on the next artifact rename). Fact 1 removed entirely, with a Split-history section recording the 2026-07-27 ruling and the verbatim clarification so nobody re-files it — Fact 1 stays welded to ISSUE-020's acceptance. BUNDLE-021 dropped from cross-refs (Fact-1-only); DIR-001 downscoped to one line. Verification criteria stripped to the two baseline-specific ones.
- **DIR-003 — done.** `ISSUE-086` added to `source:`; Description extended by one sentence (baseline job must install packs before generating). New Notes section carries both load-bearing facts: (a) the directive cannot be worked to completion off the current packless artifact, stated as position-independent gating, and (b) the approved coverage-baseline refresh is HELD (founder-ratified 2026-07-27) until the baseline job installs packs — recorded as a founder hold, not a PM suggestion, so it can't be quietly lifted. Status left `queued`; BACKLOG.yml untouched. `artifact validate --directive DIR-003` → all checks passed.
- **DIR-025 — done.** Hard-coupling claim retired; the "Positioned directly after DIR-001" Notes sentence retired with a dated retraction and NOT replaced by a new position claim. Founder's ruling quoted verbatim, with the reasoning recorded in three parts: half 2 reconstructable from GitHub + Homebrew analytics (both baked in day one), half 1 near-zero value at launch scale, and trust-first design reinforced by an unhurried BUNDLE-016 timeline. No reorder — DIR-025 stays at position 8. Validates clean.
- Corpus-wide note: `artifact validate --all` is exit 1, sole failure `SPEC-057` missing title/verification/requirements/claims — mid-authoring in another session, not caused by any of the above.
- Needs from Brandon: nothing — FYI.

## PROPOSAL confidence pass — 2 directives' Notes went stale today; 2 cluster issues are actually delivered (2026-07-27T22:47Z)
- Classification: reverse-direction reconciliation against ~20 commits. Nothing applied — all of this is directive-author work I have not started.
- **BACKLOG.yml membership is CLEAN.** 13 entries = exactly the 13 non-terminal directives. No `done`/`replaced` leakage, nothing missing. The `bundles:` section is unchanged at 7 (BUNDLE-009/010 still absent — that escalation stands below).
- **DIR-026 Notes are now FALSE.** They name REQ-039 (version/identity validation) as "the highest-value remaining seed" and say the remaining seeds "have not yet been specced." SPEC-056 implemented REQ-039@1.1.0 today — v1.1.1, `status: implemented`, plan completed, 12+ claims carrying `supports: pack-distribution-lifecycle:REQ-039@1.1.0`. Separately **SPEC-056 is cited by no directive at all.** Recommend: cite SPEC-056 in DIR-026's `source:` and rewrite the REQ-039 bullet to delivered, leaving transactions/parity as the actual remainder. Worth re-asking whether DIR-026 is now close enough to `done` to warrant the close verification — ISSUE-083 is cited but explicitly post-launch and not-to-be-planned, so it may not be holding the directive open on merit.
- **DIR-019 Notes read forward-looking for work that closed today.** The sequencing paragraph still says "ISSUE-079 is next work under this directive, ahead of ISSUE-081" — 079 is `closed`, plan completed. The ISSUE-085 paragraph still reads as a decision awaiting execution ("Founder-decided 2026-07-27: direction 1 — a new `recipes`/scaffolding archetype, WITH TEETH") — that shipped and 085 is `closed`. Directive status `active` is still correct (ISSUE-080's data-loss remainder and ISSUE-081 Gap 3 are genuinely open). Recommend a Notes refresh only, no status change.
- **ISSUE-070 and ISSUE-075 are status-drifted, and it shrinks my own escalation.** Both have merged fixes (`43ddeb6` for 070, `37bdb75` for 075) and `completed` plans, but both are still `status: open` with no Resolution section. This is pre-existing drift, not from today. It matters because both sit inside the gate-verdict-honesty cluster I escalated as four orphaned issues — if 070 and 075 close, that cluster is ISSUE-066 + ISSUE-067 only, which is a much weaker case for a whole new directive and a much stronger case for appending two issues to DIR-024 (which already cites 075 and 077). Recommend verifying and closing both before deciding the cluster's home.
- **ISSUE-081 is correctly open** — Gaps 1 and 2 delivered, Gap 3 (insert placement semantics) explicitly still open in the body. No drift.
- **Uncited open issues: 10** (was 15 at the 2026-07-26 sweep): 065, 066, 067, 070, 071, 072, 076, 088, 089, 090. The first six are all sitting in escalations already below. 088 (local-pack `.git` residual) and 089 (validate discards positional path) are today's filings with no home yet; SPEC-057 is being authored against 088 and is likewise cited by no directive. 090 is the resolver fix — team lead reports it is being homed under DIR-001, not yet reflected in the corpus.
- **Unchanged drift from the 2026-07-26 sweep:** DIR-006/007/011/015 are `done` with no `directive.completed`; DIR-002's body still describes language detection and auto-installing semgrep/golangci-lint/ruff, which contradicts BUNDLE-003 OQ-5 and reads as a baked-language violation. Both still need your authorization.
- Needs from Brandon: authorize (a) the DIR-026 Notes/source correction, (b) the DIR-019 Notes refresh, (c) verifying + closing ISSUE-070/075, which then reshapes the gate-honesty cluster decision.

## RESOLVED (2026-07-27, orchestrator relay): ID-reservation drift — founder ruled "backfill + fix resolver"; backfill EXECUTED and verified by me. Issue tags now number 90 with max `backstop/issue/090` against 87 issue files, and every other type's max tag leads its file count (spec 062/46, bundle 030/21, directive 031/26, adr 018). Burnt tags left deliberately. The collision hazard on adding the `backstop-ai/backstop-core` remote is therefore CLOSED — tags lead files everywhere, so the tag path can no longer re-issue a used number. The resolver fix itself is ISSUE-090 (see below), filed but not yet cited by any directive.
## ESCALATION ID-reservation drift — adding the real remote will re-issue ISSUE-089 (2026-07-27T13:55Z)
- Classification: no-fit — latent defect found while triaging ISSUE-090; needs an issue filed (I can't file one; the trigger hook would recurse)
- Action taken: none. Nothing touched.
- The defect: `GitTagResolver.Resolve` (`pkg/scaffold/idresolver.go:73`) allocates `max(existing backstop/<type>/NNN tag) + 1`, reading git tags ONLY — it never looks at disk. `ChainedResolver` (`idresolver.go:198`) falls back to `LocalScanResolver` (filesystem `ReadDir`, `idresolver.go:161`) whenever git ops fail, including `FetchTags` (`:78`) and tag push (`:148`). This repo has **no remote configured**, so every recent scaffold has silently taken the fallback path. Result: 88 issue tags on disk (001–088) vs 89 issue files — ISSUE-089 is committed with no reservation tag, and ISSUE-090 was allocated the same tagless way. Today that's harmless (local scan gives correct sequential IDs). It stops being harmless the moment you add the real `backstop-ai/backstop-core` remote — a named launch task. At that point `FetchTags` succeeds, the tag path reactivates, finds max tag 088, and hands out **089** to the next new issue, colliding with the committed ISSUE-089. Same hazard for any type whose tags lag its files; I only verified issues.
- Recommendation: file this as a bug against DIR-023 (Pack Distribution Hardening) or DIR-001 — I'd argue DIR-001, because the trigger is literally "add the remote," which is DIR-001/launch work, and the fix wants to land before that step rather than after. Cheapest correct fix is to make the resolver take `max(tags, local scan)` instead of tags-alone, which also makes the fallback non-lossy. Cheaper stopgap if you'd rather not touch code before launch: backfill `backstop/issue/089` (and `/090` if you keep that ID burned) as annotated tags before adding the remote. Note the stopgap is per-type and has to be redone for any other artifact type with the same lag.
- Needs from Brandon: (a) confirm DIR-001 vs DIR-023 as the home, and (b) tell me to have issue-author file it — or file it yourself. Worth deciding before the remote gets added, not after.

## RESOLVED (2026-07-27, orchestrator relay): ISSUE-090 stub removed — but note the ID was REUSED, not retired. The empty stub was rm'd (untracked, never tagged), and ISSUE-090 now exists as a real, committed, substantive issue (`bad2178`, "ID resolver must take max(tags, disk) — fallback allocations are tag-lossy"), i.e. the resolver fix from the escalation above. So the number was recycled rather than burnt. Flagging only so nobody later reads the two INBOX entries and expects 090 to be a dead ID: it is live and tagged. It is not yet cited by any directive — DIR-001 is the reported intent.
## ESCALATION ISSUE-090 — duplicate stub, self-retracted; recommend deleting the file (2026-07-27T13:55Z)
- Classification: no-fit — zero substance; the artifact is its own retraction notice
- Action taken: none. No directive touched, no slot, BACKLOG.yml untouched.
- What it is: the file contains no frontmatter and no issue body — just a note saying it was scaffolded in error on 2026-07-27 and that the real defect (`go.core.error-type-suffix` cross-declaration FP) belongs to ISSUE-061. I verified every claim it makes and they all hold: ISSUE-061 exists, is `open`, and has been amended today with the new SPEC-056 phase-5 evidence (line 41, "a second occurrence surfaced"; line 58, the minimal two-file repro) — that amendment is currently uncommitted (` M`). ISSUE-061 is already cited by DIR-021 (`source:` line 13), which flags it as the one hard-deadline item in an otherwise deadline-free directive. So the substance is correctly homed and needs nothing from me. ISSUE-090 itself is untracked (`??`), never `git add`ed, and has no `backstop/issue/090` tag.
- Recommendation: delete it — `rm issues/ISSUE-090-go-standards-error-type-suffix-cross-declaration-fp.issue.md`. I did not run this myself: deleting from the artifact corpus is a corpus mutation and my grant covers slotting and bundle-appends, not deletions. Deleting is safe on ID grounds *today* (local-scan allocation would then re-issue 090 to the next issue, no collision) — but read the ID-reservation escalation above first, because that interacts with whether you want 090 left burned. If you'd rather not think about it, leaving the stub in place is also fine: it's untracked, so it can't reach the repo, and it doesn't affect `artifact validate --all` unless something globs it.
- Needs from Brandon: a yes/no on the `rm`. Nothing else.

## FYI ISSUE-087 — slotted into DIR-001 under the standing grant (2026-07-27T11:15Z)
- Classification: clear-fit DIR-001 (Release Workflow)
- Action taken: slotted via directive-author — `ISSUE-087` added to DIR-001's `source:`, Notes record it as the launch-minimal slice; status left `queued`, BACKLOG.yml untouched. `artifact validate --directive DIR-001` passes.
- Recommendation: no ambiguity here — you tiered CI-driven releases launch-blocking on 2026-07-27 and named DIR-001 the anchor in the issue body itself. The Notes now pin the two carve-outs so they can't read as oversight later: self-gating via `backstop gate` (needs a macOS runner + ISSUE-020's Linux sandbox for matrix parity) and the Homebrew tap both stay open under DIR-001 after ISSUE-087 closes. One thing worth your eye: ISSUE-087's tag-integrity workflow lands hand-written with a placeholder header pointing at DIR-019/REQ-018's CI recipe pack — that's the *third* hand-baked-CI instance in play (ci.yml gate job per ISSUE-086, pack-repo CI per ISSUE-084, now this). It's the right call for a 1–1.5 day launch slice, but the placeholder only stays honest if REQ-018 actually lands; if recipes slip, this becomes permanent debt by default.
- In-flight coverage: **live right now.** `plans/PLAN-ISSUE-087-...plan.yml` was scaffolded 11:11Z (one minute after the issue), currently an empty `phases: []` stub — so `/plan` is mid-flight in the session that filed the issue. It fails `artifact validate --all` in its stub state; that's expected mid-authoring, not a defect, but it means `--all` is red in the tree until that plan is written. No release code exists yet: no `.goreleaser.yml`, `ci.yml` is the only workflow, module path still `bmanson/`.
- Needs from Brandon: nothing — FYI.

## PROPOSAL BACKLOG.yml — DIR-025 (Telemetry) now sits 3 slots behind DIR-001, contradicting its own hard coupling (2026-07-27T11:15Z)
- Classification: priority — proposed reorder, NOT applied
- Action taken: none. Diff below for your call.
- The conflict: DIR-025's Description states the coupling as a hard constraint — "This directive must land WITH OR BEFORE DIR-001 (Release Workflow)… telemetry collection that starts after day one of the first public release loses the day-zero baseline permanently" — and its Notes assert it is "positioned directly after DIR-001 in BACKLOG.yml." That placement was true before 2026-07-27; it isn't now. DIR-001 moved up to position 5 and DIR-025 stayed at 8, so DIR-003 and DIR-002 sit between them. ISSUE-087 is what makes this bite: it scopes DIR-001 to ~1–1.5 days of launch-minimal work, which turns "day one of first release" from an abstraction into something reachable in this cycle.
- Proposed diff (`directives:` list): move `DIR-025` from position 8 to position 6, directly after `DIR-001` — i.e. `… DIR-024, DIR-001, DIR-025, DIR-003, DIR-002, DIR-021, …`. Nothing else moves.
- Caveat I want on the record: BUNDLE-016 is still `exploring` with unpopulated Overview/Components, so DIR-025 cannot be spec'd today regardless of position — moving it up buys ordering honesty, not throughput. The genuine alternative is the opposite move: accept that day-zero telemetry is not happening, edit DIR-025's Notes to drop the stale "directly after DIR-001" claim, and leave it at 8. What's not defensible is leaving both the hard-coupling text and the current position as-is — one of them is lying.
- Needs from Brandon: pick one — (a) move DIR-025 to position 6 and drive BUNDLE-016's OQs before the release tag, or (b) leave the order and let me route a Notes correction through directive-author retiring the coupling claim.

## ESCALATION ISSUE-086 — half of it re-files what you said not to re-file; the other half is new (2026-07-27T11:00Z)
- Classification: partial-duplicate + ambiguous (Fact 1 → ISSUE-020/DIR-024 already; Fact 2 → DIR-003 vs DIR-001)
- Action taken: none — no slot, no reorder. Not a clear fit for exactly one directive, and the duplicate half needs your ruling before anything is cited.
- **The conflict.** ISSUE-086 Fact 1 (core `ci.yml`'s `gate` job hand-bakes Go instead of calling `backstop gate`) is *already* inside ISSUE-020, deliberately, with your ruling recorded verbatim in its body (2026-07-26): "this does not get its own issue… filing a separate issue-and-plan for pointing a CI step at `backstop gate` instead of raw tool invocations would be ceremony… **Do not re-file this as a standalone issue.**" It is folded into ISSUE-020's acceptance criterion — "`backstop gate` runs green in CI on Linux" — precisely so the sandbox fix can't be claimed done without the CI wiring. ISSUE-086 cites ISSUE-020 five times but never mentions that ruling, which reads to me as the ruling not being in view at filing time rather than a considered override. The 2026-07-27 quote it leans on ("yes i am aware of that. that'll be a launch checklist item") acknowledges the *facts*; it doesn't obviously authorize a standalone re-file.
- **Fact 2 is genuinely new.** The `baseline` job (`ci.yml:39-64`) has no `pack install` step, so the published `backstop-baseline-v1` artifact is generated with zero pack engines present — every semgrep/ast-grep dimension structurally absent, not clean. ISSUE-020 notes that job builds and runs the binary but says nothing about the missing install or the packless artifact. Sharp consequence: refreshing the tracked baseline against that artifact would ratchet-declare every pack-engine dimension clean without ever evaluating it — vacuous green of exactly the kind the project exists to prevent.
- In-flight coverage: no implementation lane. ISSUE-086 self-describes as a tracker with no code change; DIR-001/DIR-003 are both `queued`; no CI plan artifact exists. Session `b45e79da` (which edited ISSUE-020 and filed this) is analysis/filing. Caveat: I could not fork-interview it — `claude --resume` was blocked by the sandbox in this headless run, so that read is corpus-based, not interview-confirmed.
- Recommendation: **split it.** Narrow ISSUE-086 to Fact 2 only (retitle to the packless baseline), delete Fact 1 from it, and cite the narrowed issue in **DIR-003** — CI baseline generation is DIR-003's literal charter, and Fact 2 breaks DIR-003's own premise that gate can trust the published artifact. Leave Fact 1 where you put it, in ISSUE-020 under DIR-024. That preserves both your rulings: the CI wiring stays welded to the sandbox acceptance bar, and the new defect gets the ID it deserves. The alternative — closing ISSUE-086 wholesale as superseded — loses Fact 2, which nothing else tracks.
- Needs from Brandon: (1) does 2026-07-27 override the 2026-07-26 do-not-re-file ruling, or does Fact 1 come out of ISSUE-086? (2) if the narrowed issue survives, DIR-003 (my rec) or DIR-001? (3) a hold on the pending coverage-baseline refresh until the baseline job installs packs — refreshing against today's artifact ratchets a vacuum.

## PROPOSAL BACKLOG.yml — no reorder; current order already encodes ISSUE-086's chain (2026-07-27T11:00Z)
- Classification: priority (no-op, recorded so the non-move is deliberate)
- Action taken: none. Explicitly proposing you change nothing.
- Recommendation: ISSUE-086 documents the chain BUNDLE-021 OQ-2/OQ-3 → ISSUE-020 (DIR-024, pos 4) → CI runs `backstop gate` (DIR-001, pos 5) → trustworthy baseline (DIR-003, pos 6). The list is already in exactly that order, and BUNDLE-021 already leads the bundles tier. Nothing to move. The one thing this issue *does* change is a non-ordering constraint: DIR-003 can't be worked to completion off the current CI artifact regardless of its position, because that artifact is packless — that's a gating fact, not a priority fact, and it belongs in DIR-003's Notes if you accept the slot above.
- Needs from Brandon: nothing — FYI, unless you disagree that the order already holds.

## RESOLVED (Brandon, 2026-07-27): ISSUE-083 home RATIFIED at DIR-026 "Remote Dependency Assembly" — founder override of the PM's DIR-023 recommendation; thematic grouping wins. The slot, its guards (post-launch, not before REQ-039, no planning until a resolution model is chosen), and the citation stand as-is. Contradiction flag closed: the prior slot lacked a ruling, this is the ruling.

## CONTRADICTION FLAG (2026-07-27T22:47Z, superseded by ruling above) — this escalation is reported to me as still open with you, but the corpus says it was already decided. DIR-026's `source:` now lists `ISSUE-083`, and its Notes carry two explicit guards traveling with the citation: ISSUE-083 is post-launch (every launch pack is first-party on `github.com/backstop-ai`, so host-generality has zero pre-launch exposure) and must not be picked up ahead of REQ-039; and it must not be planned at all until you pick among its three candidate resolution models, since full-URL-as-coordinate would reopen the `name == coordinate` convention. That is DIR-026, not my recommended DIR-023. I have NOT touched it either way — if the slot was intended, this escalation is closed and my recommendation was overridden; if it wasn't, someone slotted it without your ruling and it needs backing out. One word from you settles which.
## ESCALATION ISSUE-083 — remote pack resolution hardcodes github.com; ambiguous home (2026-07-27T00:06Z)
- Classification: ambiguous (DIR-023 vs DIR-026; BUNDLE-001 OQ-30 is the publish-side twin)
- Action taken: none — awaiting Brandon. No slot, no reorder.
- Substance: `resolveGitURL` (`pkg/pack/distribution/add.go:95`) returns `"https://github.com/" + name + ".git"` — the single choke point for every remote flow (add/update/upgrade + `versionresolver.go:70` tag listing). Inert until today: pre-SPEC-055 the remote path panicked (ISSUE-073). Now that it clones for real and DIR-027 ratified `<org>/<pack-name>` as pack identity, a third party cannot author an installable pack unless they publish it to GitHub. BUNDLE-006 DD-31 already accepted "packs may be hosted anywhere" for lock *recording*; resolution contradicts that premise.
- In-flight coverage: implausible, corpus-based check only — session transcripts were unreadable from this headless run (sandbox blocked `~/.claude/projects`). SPEC-055 deliberately kept the shorthand; DIR-027's four threads are enumerated and silent on host generality; the issue was surfaced by my own post-publication review hours ago.
- Recommendation: **DIR-023 (Pack Distribution Hardening)**, not DIR-026. DIR-026 is the launch blocker with a narrow remaining scope (REQ-039, transactions/parity) — appending non-blocking capability expansion to it dilutes the blocker signal, and "make GitHub work" is done. DIR-023 is queued and explicitly framed "on the road to the registry era"; ISSUE-058's catch-22 and this share one root cause — core has no data source for where a pack lives, so it bakes one. A registry/index is the thing that would retire the hardcode. Same *shape* as the zero-baked-knowledge invariant (an ecosystem assumption baked into core) though not a strict violation as CLAUDE.md scopes it. Not launch-blocking: our fleet and ICP are on GitHub, no known non-GitHub consumer. If you'd rather it stand alone, the honest framing is a "pack ecosystem openness" directive pairing it with BUNDLE-001 OQ-30 (non-GitHub *publisher* support, open) — one host decision, both sides; deciding resolution without publication leaves the story half-open.
- Needs from Brandon: DIR-023 (my rec) vs DIR-026 vs new openness directive vs leave uncited until a real non-GitHub consumer appears. Also note for reading the entries below: they propose a **DIR-027 "Gate Verdict Honesty"** that was never created — DIR-027 is now "Pack Fleet Publication & Migration". That gate-honesty cluster is still unhomed and needs a fresh number.

## FYI full sweep 2026-07-26 — corpus reconciled; 2 slots applied, 6 escalations open (2026-07-26T23:20Z)
- Classification: sweep summary
- Action taken: write path CONFIRMED WORKING — directive-author dispatches now execute. Two clear-fit slots applied (DIR-019, DIR-022); `artifact validate --all` passes.
- Scope of sweep: 82 issues, 26 directives, 25 bundles, BACKLOG.yml. Found **15 open issues cited by no directive**; 13 of the other open issues are correctly homed. BACKLOG.yml membership is CLEAN — all 12 non-terminal directives listed, zero `done`/`replaced` leakage, no missing entries.
- Needs from Brandon: the 6 escalations below (ISSUE-079 status, gate-honesty cluster, ISSUE-065, ISSUE-071/072, ISSUE-076/077, bundles-section gap) + 2 carried over (ISSUE-074, ISSUE-080).

## FYI ISSUE-079/081 + SPEC-054 — slotted into DIR-019 under the standing grant (2026-07-26T23:20Z)
- Classification: clear-fit DIR-019 (only directive in the corpus chartered for pack recipes)
- Action taken: slotted via directive-author. `source:` = BUNDLE-015, SPEC-054, ISSUE-079, ISSUE-081. Also rewrote the now-FALSE gating sentence ("BUNDLE-015 is still at `exploring`… no spec work begins until it progresses past `exploring`") — BUNDLE-015 is `defined` and SPEC-054 is `implemented`. Added Notes: `recipes/` holds three EMPTY subdirs (go/meta/typescript) and no pack fleet-wide declares a `recipes:` block. Validates clean.
- **In-flight coverage: PROVEN ABSENT.** ISSUE-079 is NOT covered by any live lane. Session `3826af56` ("recipe bundle") is stale — last turn 2026-07-24, still awaiting a go to `/plan` SPEC-054, and has never referenced 079/080/081. Session `b45e79da` knows all three by ID but has done zero implementation and is self-embargoed pending your sweep. `PLAN-SPEC-054` references none of them. ISSUE-079 is genuinely orphaned, not merely uncited.
- Needs from Brandon: nothing on the slot — FYI. But see the ISSUE-079 escalation below; risk:critical is not sitting behind an owner.

## RESOLVED (Brandon ack, 2026-07-26): ISSUE-079 / DIR-019 — status flipped queued→active via directive-author, ISSUE-079 recorded next-up ahead of ISSUE-081, validates clean. Remaining from this escalation: the SPEC-054 substitution-scope amendment rides with the 079 fix itself — CONFIRMED by Brandon (2026-07-26): spec-first, table before code; whichever session picks up ISSUE-079 starts with the spec-author touch-up.
- Classification: priority (now homed, but unowned and mis-signalled)
- Action taken: homed into DIR-019 (above). Status flip NOT applied — not granted.
- Substance: `pkg/recipe/apply.go` routes only payload/fragment CONTENT through `Substitute`; every SITE field (`Op.Target` for create/merge/transform/insert, `Op.Anchor`, `Op.Snippet`, and `siteFor`'s `InjectionSites` override) is used raw. `recipe apply` wrote a literal `./{{ config_dir }}/service.json` directory **and exited 0**. Silent wrong output on launch-blocker #1.
- Why it hid: every `TestSubstitute_*` test calls `Substitute()` directly, and every `apply_*_test.go` builds ops inline with non-templated sites. The one fixture that templates targets is parsed, never applied.
- Recommendation: (1) flip DIR-019 `queued` → `active` — SPEC-054 is implemented and defects are open against it; `queued` reads as "not started" on the #1 launch blocker. (2) ISSUE-079 is the next work under DIR-019, ahead of ISSUE-081 (which is authoring-surface polish). (3) Its fix needs a SPEC-054 amendment — a substitution-SCOPE table — because the spec's §"Substitution (REQ-002)" never pins scope, so the spec is a contributing gap, not just the code.
- No BACKLOG.yml reorder proposed: DIR-019 already holds position 1.
- Needs from Brandon: ack the `queued`→`active` flip, and confirm ISSUE-079 is next-up under DIR-019.

## RECOMMENDATION CHANGED (2026-07-27T22:59Z) — this escalation is now 2 issues, not 4, and I no longer recommend a new directive.
- **What changed:** ISSUE-070 and ISSUE-075 were never open work — both fixes were merged and their plans completed; they were status-drift, and both are now `closed` with Resolution sections and verified evidence (see the follow-ups entry at the top). The cluster is **ISSUE-066 + ISSUE-067 only**.
- **My recommendation is now: append ISSUE-066 and ISSUE-067 to DIR-024, and do NOT create a new directive.** Two issues do not justify a directive of their own, and DIR-024 already cites ISSUE-075 and ISSUE-077, so it is demonstrably the place this class of work has been landing. The original case for a standalone directive rested substantially on there being four issues with a shared "the gate's verdict is wrong" theme and enough mass to stand alone; at two, the "don't bury the Linux launch blocker under a growing catch-all" objection to DIR-024 outweighs the thematic tidiness of a new one.
- **What has NOT changed:** ISSUE-067 is still `risk: critical` — the go-test engine reports a real `--- FAIL:` as an opaque dispatch crash, so genuine regressions are indistinguishable from tool noise and get dismissed. That is the item worth your attention regardless of which directive holds it. ISSUE-066 (gate honors a plan's narrow `-run` filter, so regressions outside the pattern stay invisible) is the milder sibling.
- **Numbering note, still live:** the entries below propose a "DIR-027 Gate Verdict Honesty". **DIR-027 is taken** — it is Pack Fleet Publication & Migration. If you overrule me and want a standalone directive anyway, it needs a fresh number (DIR-028 by current allocation).
- Needs from Brandon: append 066+067 to DIR-024 (my revised rec) vs. a new DIR-028 vs. leave orphaned. The text below is the ORIGINAL 4-issue framing, kept for the record.

## ESCALATION gate-verdict-honesty cluster — 4 orphaned issues, new-directive candidate (2026-07-26T23:20Z) — SUPERSEDED, see above
- Classification: no-fit → new directive recommended (draft only; nothing touched)
- Artifacts: ISSUE-066 (gate honors a plan's narrow `-run` filter, so regressions outside the pattern stay invisible), ISSUE-067 (**risk:critical** — go-test engine reports real `--- FAIL:` as an opaque dispatch crash, so regressions are indistinguishable from tool noise and get dismissed), ISSUE-070 (diff-scope leaks project-wide lint: 113 violations across 30 files when only 2 were in a 19-file diff), ISSUE-075 (smoke test `TestSmoke_GateFailsMissingMandatedTest` is itself a false green — hardcoded `status: draft` filtered out by ISSUE-054 scoping; hid 12 days).
- Why no fit: all four are "the gate's verdict is wrong" — the charter DIR-015 held, which is `done`. DIR-024 is the nearest but self-describes as gaps "that don't fit the other three directives' themes" and carries a hard rule that ISSUE-020 lands first; appending four more would bury the Linux launch blocker underneath a growing catch-all.
- Recommendation: new **DIR-027 "Gate Verdict Honesty"** citing ISSUE-067, ISSUE-066, ISSUE-070, ISSUE-075. This is the "no vacuous green" invariant policing itself — three of the four make the gate lie in the green direction, which is the exact failure mode the product sells against. Suggested placement: directly below the three launch blockers (position 4), above DIR-003. Not applied — new directives and ordering are both yours.
- Needs from Brandon: create DIR-027 (I'll draft via directive-author on your word) vs. append all four to DIR-024 vs. leave orphaned.

## ESCALATION ISSUE-065 — ambiguous home (2026-07-26T23:20Z)
- Classification: ambiguous (DIR-022 vs the gate/engine surface)
- Action taken: none — awaiting Brandon
- Substance: `contractSignatureEngine` (`cmd/backstop/gate.go`) selects the contracts engine by hardcoded key `"ast-grep-contracts"` with an `"ast-grep"` fallback, so a pack declaring it under any other key silently falls through. No declared field distinguishes signature-match from absence-probe today.
- Recommendation: DIR-022. Its thread 1 is already scoped to introducing a new declared engine `input_mode` + matching detection path, and ISSUE-065's fix is the same shape (a new declared capability/role). Counter-argument: substantively this is a baked-name violation, whose natural home DIR-014 is `done` — and per the ISSUE-082 precedent we don't reopen `done` directives.
- Needs from Brandon: DIR-022 (my rec) vs. fold into the proposed DIR-027 vs. leave.

## ESCALATION ISSUE-071 + ISSUE-072 — artifact-lifecycle pair, ambiguous (2026-07-26T23:20Z)
- Classification: ambiguous (DIR-021 nearest; DIR-016 is the true charter but `done`)
- Action taken: none — awaiting Brandon
- ISSUE-072 (sub-artifact retirement primitive + claim versioning): recommend **DIR-021**. Its ISSUE-048 thread is the stranded-terminal-lineage strand that 072's retirement primitive would actually close — they're the symptom and the mechanism.
- ISSUE-071 (issue closed with zero mandated tests passes vacuously): recommend **leave uncited for now** — the issue itself says "do not schedule ahead of" the recipe/init chain, and honoring that self-deferral is cleaner than homing it into a directive it would then sit at the bottom of.
- Needs from Brandon: ack DIR-021 for 072; confirm 071 stays deliberately unhomed.

## ESCALATION ISSUE-076 + ISSUE-077 — no home; agent-toolchain trust defects (2026-07-26T23:20Z)
- Classification: no-fit (no directive covers the dev/agent toolchain)
- Action taken: none — awaiting Brandon
- ISSUE-076: `backstop code check` was deleted by ISSUE-018 yet is still mandated in **16 places** in the in-flight `PLAN-SPEC-054` (which mentions `go test` zero times) and in `.claude/agents/implementer.md` as the implementer's inner loop. Currently masked only by an agent-memory file.
- ISSUE-077: bare `backstop` resolves to a stale `/usr/local/bin/backstop` (3 binaries found, up to 7 days apart), so gate runs execute stale code and attribute phantom violations to a clean diff. A false-RED trust hole an agent cannot self-heal. CLAUDE.md's `./bin/backstop` convention is unenforced tribal knowledge that has already leaked into plan artifacts.
- Why they matter more than "ergonomics": both make the gate lie to the agent that depends on it — 076 mandates a command that cannot run, 077 grades the wrong binary. This is `project_prompts_are_vibes` in the concrete: a convention that only exists in prose is already drifting into artifacts.
- Recommendation: pair them under the proposed DIR-027, or a small separate directive. ISSUE-077 explicitly names BUNDLE-020 (pack↔core version compatibility) as its consumer-facing sibling **with an instruction not to fold either scope into the other** — so BUNDLE-020 is adjacent, not a home.
- Needs from Brandon: pick a home, or ratify leaving both orphaned with eyes open.

## PROPOSAL directive status + metadata drift (2026-07-26T23:20Z)
- Classification: priority / reverse-direction reconciliation. Nothing applied.
- **No queued directive has all sources closed/superseded** — the reverse-direction check comes back clean on that specific question. What it did surface:
- **DIR-026** — `source:` SPEC-055, now `implemented`. The directive's own rule says flip to `done` (with `directive.completed`) "only once PLAN-SPEC-055 finishes and the gate is proven green against it." SPEC-055 reaching `implemented` means that condition is likely met — worth a verification pass and, if green, a close. Launch blocker #2 may be done and mislabelled.
- **DIR-019** — still `queued` while its spec is implemented (see ISSUE-079 escalation).
- **DIR-002** — body is materially stale: "detects language, auto-installs dependencies (semgrep, golangci-lint, ruff)" is directly contradicted by BUNDLE-003 OQ-5 (resolved: zero day-zero language detection) and by DIR-019's delegate-everything-to-packs resolution. It also reads as a baked-language violation of the first principle in CLAUDE.md. Recommend re-authoring before any spec is written against it.
- **DIR-006, DIR-007, DIR-011, DIR-015** — `done` but carry no `directive.completed` field, unlike the other seven done directives. Lifecycle-metadata drift; cheap to backfill.
- **DIR-024** — lede says "Two gate/engine-quality gaps" then enumerates three (ISSUE-082 was appended without updating the count). Cosmetic.
- Needs from Brandon: authorize (a) the DIR-026 close verification, (b) the DIR-002 re-author, (c) the `completed`-field backfill. All three are directive-author work I have not started.

## PROPOSAL bundles: section is missing entries — propose-only (2026-07-26T23:20Z)
- Classification: priority / bundle reconciliation. NOT applied — you ratified the current 7-item list on 2026-07-26, and retroactively adding to a founder-ratified list is a ranking act, not the granted append-on-creation.
- **Uncited by ANY directive and absent from the section:** BUNDLE-009 (Stack-Aware Traceability — Substantiveness & Contracts beyond Go, `ready`) and BUNDLE-010 (Pluggable Pack Engines, `defined`). Both are non-terminal, both are real expressions of intent, and BUNDLE-009 at `ready` is the most advanced unhomed bundle in the corpus.
- **Homed-but-orphaned** (cited only by `done` directives, so they read as covered while nothing active carries them): BUNDLE-004 (`ready`), BUNDLE-005 (`ready`), BUNDLE-008 (`exploring`). Your ruling was "when a directive later cites a bundle, remove it from the section" — it didn't say what happens when that directive finishes. This is the edge case.
- **Legacy:** `bundles/cli.bundle.md` (`defined`, schema v1, no BUNDLE-NNN id) is invisible to every ID-based check in the corpus, including this sweep's greps.
- Recommendation: append BUNDLE-009 and BUNDLE-010 to the section (you pick position); rule on whether done-directive citation still counts as homed; retire or renumber `cli.bundle.md`.
- Needs from Brandon: the append + the done-directive-citation rule.

## CARRIED OVER — still awaiting Brandon
- **ISSUE-074** (2026-07-26T17:35Z) — home + open-status decision. Unchanged; still uncited. Full detail below.
- **ISSUE-080** (raised 22:40Z) — held as ambiguous between DIR-019 and DIR-026 (its root cause is the shared `ExitViolations`/`main.go` stderr-suppression convention that BUNDLE-006's seeds claim by name — the same root cause as ISSUE-074). I deliberately did NOT slot it with 079/081 this sweep, and recorded it in DIR-019's Notes as an open question rather than as owned scope. Note it has a third finding filed 2026-07-26: a malformed waiver reason code is dropped, so the applier **clobbers the operator's diverged file and exits 0 saying "applied recipe."**

---

## PROPOSAL full sweep — post-SPEC-054/055 launch-readiness review (2026-07-26T22:40Z)
- Classification: priority + clear-fit (blocked on write path — slot NOT applied)
- Action taken: none applied. The directive-author dispatch to slot SPEC-054/ISSUE-079/ISSUE-081 into DIR-019 was REFUSED by the permission classifier, so the clear-fit slot the standing grant authorizes could not be executed. Paste-ready brief is in the sweep report.
- **SUPERSEDED 2026-07-26T23:20Z:** the write path now works; recommendations (1) and (3) are executed/honored. (2) the status flip remains open. (4) still stands.
- Recommendation: (1) slot SPEC-054 + ISSUE-079 + ISSUE-081 into DIR-019 `source:`; (2) flip DIR-019 `queued` → `active` (SPEC-054 is implemented; the "no spec work until BUNDLE-015 leaves exploring" sentence is stale); (3) hold ISSUE-080 — ambiguous between DIR-019 and DIR-026, whose BUNDLE-006 seeds claim the shared ExitViolations convention by name; (4) no BACKLOG.yml reorder proposed for the top three.
- Launch finding: both tier-1 capabilities are MECHANISM-complete and ECOSYSTEM-empty. Zero packs declare a `recipes:` block (REQ-018 CI recipe pack unbuilt); every lock entry in core, bclabs-portal, stash, and backstop-harness is `source_type: local` — zero remote installs exist fleet-wide, and the `backstop/*` pack names cannot resolve to real GitHub coordinates.
- Needs from Brandon: decide (1)-(3) above; unblock the PM write path or accept paste-ready output as the steady state.

## RESOLVED (Brandon, 2026-07-26): unhomed bundles policy + BUNDLE-021
- Ruling: ALL bundles are expressions of intent and belong in the backlog regardless of maturity. BACKLOG.yml now has a `bundles:` section (schema extended); all seven unhomed bundles listed, ordering proposed and awaiting Brandon's reorder. BUNDLE-021: listed there, uncited by any directive until `defined` (PM recommendation adopted).

## ESCALATION BUNDLE-021 — no directive home (2026-07-26T18:05Z)
- Classification: no-fit (nearest = DIR-024, which explicitly disclaims this scope)
- Action taken: none — awaiting Brandon (standing grant correctly NOT invoked)
- Recommendation (better-grounded of two runs): leave BUNDLE-021 uncited until it promotes to `defined` — work breakdown structurally undetermined across OQ-1..4; a directive now fabricates scope. DIR-024's Description explicitly assigns this question OUT to BUNDLE-021. (Earlier 082-run suggested a new "Pack Execution Trust Boundary" directive below launch blockers — alternative if you want it homed now.)
- Sequencing FYI (no reorder proposed): if ISSUE-020 gets planned before OQ-3 resolves, the Linux sandbox re-derives a profile contract this bundle would supply.
- Needs from Brandon: (1) uncited-until-defined vs new DIR-027 vs amend DIR-024; (2) are unhomed `exploring` bundles fine by design? (BUNDLE-020 is also uncited.)

## FYI ISSUE-082 — slotted into DIR-024 under the standing grant (2026-07-26T17:46Z)
- Classification: clear-fit DIR-024
- Action taken: slotted via directive-author — source += ISSUE-082, Description bullet, Notes sequencing caveat. Directive validates clean.
- Note: DIR-014 was the closer substantive match but is `done` (2026-07-06); PM correctly did not reopen. Zero interviews needed.
- Needs from Brandon: nothing — FYI. Reverse if you'd rather reopen DIR-014.

## ESCALATION ISSUE-074 — home + open-status decision (2026-07-26T17:35Z)
- Classification: ambiguous (DIR-026 needs scope amendment vs DIR-023 splits delivery chain)
- Action taken: none — awaiting Brandon (grant correctly NOT invoked)
- Note: transcribed by mediator; PM's write path was blocked on first run (since fixed)

## ISSUE-074 — ambiguous, no slot taken, already covered in-flight

Grounded against BACKLOG.yml, all 25 directives, and the live tree. **Zero interviews needed** — coverage was provable from artifacts plus merged code.

**Half A (swallowed diagnostic) is already delivered.** `cmd/backstop/main.go` now carries the `reportError` seam with an `Explained` opt-out, merged, and its doc comment names ISSUE-074 by ID. That's SPEC-055 REQ-011/REQ-014 / CLM-074 / `TestExitSurfacing_PackRelock_PrintsDiagnostic`.

**Half B (path-vs-name argument) is explicitly deferred out of SPEC-055** to a later BUNDLE-006 seed as **REQ-041** — where it's load-bearing, not incidental: DD-28 designates `pack relock` as *the* migration vehicle for local-path legacy content hashes, so a relock that rejects the identifier every sibling command accepts blocks that migration story. `pack_relock.go` still declares `Use: "relock [path]"`.

**Why I didn't slot it.** DIR-026 is the right home — it already cites BUNDLE-006 and SPEC-055, the exact artifacts carrying both halves. But its Description is written narrowly around *remote* pack consumption, and ISSUE-074 is a local-relock/error-surfacing defect; citing it honestly needs a one-line scope amendment, which is a scope change rather than a clear-fit slot. DIR-023 (Pack Distribution Hardening) matches on subject matter — local-pack lock correctness — but owns neither the bundle nor the spec doing the work, and would split the issue from its own delivery chain. Not recommended.

**No BACKLOG.yml reorder proposed.** DIR-026 is already position 2 and a launch blocker; ISSUE-074 doesn't move anything.

### Needs from you

1. Pick a home: **(a)** authorize directive-author to add ISSUE-074 to DIR-026 `source:` + the one-line Description amendment *(my recommendation)*; **(b)** DIR-023 instead; or **(c)** leave it uncited, tracked only via BUNDLE-006 REQ-041.
2. Confirm ISSUE-074 stays `open` until REQ-041 lands, rather than closing on the half that shipped. Live footgun: with the silence fixed and the arg shape not, `pack relock <name>` now prints a real "no such file or directory" — SPEC-055's own sharp-edges section warns this reads like a fix that didn't happen.
3. Green-light the guard/permission amendment above. It's enforcement code, so I didn't touch it.

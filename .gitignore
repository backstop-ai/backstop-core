# Go
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Go workspace
go.work

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build output
dist/

# Node (for TypeScript packs)
node_modules/
cover.out
/backstop
cover*.out

# Scratch / handoff cruft — never committed (session-handoff-model)
tmp/
HANDOFF-*.md
HANDOFF-*.txt

# Prototype working output
prototype/
.backstop/packs/
.backstop/pack-config-provenance.json
.backstop/baseline.json

# CI RUN DETRITUS — files the workflow's own steps leave in the workspace.
#
# The gate's diff scope INCLUDES UNTRACKED FILES, and several steps run before it:
# the engine-tools step downloads ast-grep.zip and the diagnostic capture step writes
# gate-report.json. Measured on run 30386673582, which reported 11 in-scope files of
# which TWO were these — CI was gating its own build output. cover.out was already
# ignored, which is why it never appeared and is the precedent this follows.
ast-grep.zip
gate-report.json

# PM operational queue — the mechanism ships, the correspondence does not.
# Escalations/proposals to the founder are session-ops, not SDLC artifacts;
# historical snapshots remain in pre-flip history as accepted fossils.
.backstop/pm/

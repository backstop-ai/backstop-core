# Backstop Project Backlog
# Position = priority. First = highest. Reorder to reprioritize.
# See artifacts/directive/v1/backlog-schema.json for format.

# Reordered 2026-07-26 per founder direction: the three launch blockers
# (recipes / remote pack consumption / Linux+CI viability) take positions
# 1-3, ahead of everything else. Order among the three is "finish what's
# already in flight" — argue with it if that's wrong. DIR-003 (Baseline)
# moves to position 4, behind the pack work but not behind everything, per
# the founder's own words ("it's definitely behind all of the pack issues
# we're working through") — an earlier pass over-applied this to dead
# last and was corrected back to just behind the three blockers. DIR-002
# (backstop init) is explicitly tiered down out of the top-3 but otherwise
# kept near its old neighbors. Everything else preserves its prior relative
# order. See DIR-026 Notes for a caveat on the recipes/DIR-019 progress
# claim underlying this ordering.
#
# UPDATED 2026-07-27 (founder-ratified): the "positions 1-3" claim above is
# now historical, not current, and stale on two counts. DIR-027 (Pack Fleet
# Publication & Migration) has since taken position 3, pushing Linux/CI
# viability (DIR-024) to position 4. Separately, the founder tiered a
# fourth launch blocker on 2026-07-27 — CI-driven releases (DIR-001) — and
# ratified moving it to position 5 (after DIR-024, ahead of
# DIR-003/DIR-002), reflected in the list below. The current four-blocker
# set is: recipes (DIR-019), remote pack consumption (DIR-026), Linux+CI
# viability (DIR-024), and CI-driven releases (DIR-001) — see those
# directives' Description/Notes for detail.
#
# UPDATE 2026-08-02: DIR-001 and DIR-026 have since been completed and
# removed from the list per the convention below, so the paragraphs above
# are history — two of the four named launch blockers are delivered and no
# longer appear below.

# Founder ruling (Brandon, 2026-08-02): BACKLOG.yml shows OPEN items only.
# A directive that reaches status: done is removed from this list entirely
# — not sunk to the bottom, not retained as a record. Delivery history
# lives in the directive file's own status field and the artifacts that
# delivered it; this list does not double as an audit log.
#
# REORDERED 2026-08-11 (founder-directed): DIR-002 (backstop init) moves
# from position 6 to position 2, immediately after DIR-019. Verified today:
# of the five directives previously ranked ahead of DIR-002, DIR-019 is the
# only one carrying a REAL dependency into init — its unbuilt BUNDLE-015
# REQ-018 (the CI recipe pack), which init's own resolved bundle design
# (BUNDLE-003 OQ-7) needs for its "CI wired by default" commitment. It stays
# at position 1 for that reason. DIR-027, DIR-024, DIR-032, and DIR-003 were
# ranked ahead of init purely by prior priority calls with no real
# dependency into init specifically — they are now explicitly deprioritized
# below it, not because their own work stopped mattering, but because none
# of them gate init. Founder's own words: "init is kind of the last
# remaining missing piece before I can start to go more public with
# backstop" — a launch-readiness call. Relative order among DIR-027/024/032/
# 003 and everything below DIR-002 is unchanged from the prior list.
#
# REORDERED 2026-08-12 (founder-directed): DIR-002 (backstop init) moves
# from position 2 to position 1; DIR-019 (Pack Recipe Capability) drops to
# position 2. DIR-019's real blocking scope for DIR-002 — the CI recipe pack
# (BUNDLE-015 REQ-018) — was delivered 2026-08-12 (SPEC-067 implemented,
# PLAN-SPEC-067 completed, backstop-ai/ci-workflows published at v0.1.0).
# What remains open in DIR-019 (ISSUE-081 Gap 3, ISSUE-110, ISSUE-119) is
# real but does NOT block DIR-002 — it was only ever ranked ahead because it
# happened to be created there operationally, not because of a live
# dependency at this position. With the dependency cleared, the founder is
# making the position honest: DIR-002 moves to 1. Everything else keeps its
# prior relative order.
directives:
  - id: DIR-002
    title: backstop init Command
  - id: DIR-019
    title: Pack Recipe Capability
  - id: DIR-027
    title: Pack Fleet Publication & Migration
  - id: DIR-024
    title: Gate/Engine Quality
  - id: DIR-032
    title: Gate Verdict Honesty
  - id: DIR-003
    title: Baseline Implementation
  - id: DIR-025
    title: Adoption Telemetry
  - id: DIR-021
    title: Traceability Hardening & Corpus Drain
  - id: DIR-008
    title: CLI Self-Update
  - id: DIR-022
    title: Contracts Engine Hardening
  - id: DIR-023
    title: Pack Distribution Hardening
  - id: DIR-020
    title: Backstop Trace Command

# Unhomed bundles — expressions of intent awaiting directive homes.
# Position = priority within this tier. Ordering founder-ratified 2026-07-26
# (001/002 held low deliberately); reorder to reprioritize.
# 2026-07-27: 021 leads because its OQ-2 (profile shape) and OQ-3
# (engine-command extension) must resolve before ISSUE-020's Linux work
# re-derives them.
bundles:
  - id: BUNDLE-021
    title: Pack Command Execution Governance
  - id: BUNDLE-020
    title: Pack Core Version Compatibility
  - id: BUNDLE-019
    title: Runbooks
  - id: BUNDLE-017
    title: Recipe Provenance Ledger
  - id: BUNDLE-001
    title: Pack Distribution, Verification, and Review (ambitious — post-launch, held deliberately)
  - id: BUNDLE-002
    title: Pack Publishing Proxy (ambitious — post-launch, held deliberately)
  - id: BUNDLE-018
    title: Stash — Secure Local Resource Store (external repo)
  # Appended on creation 2026-07-29 per the standing bundle-append grant.
  # Position is APPEND ORDER, not a priority claim — the INBOX carries a
  # PROPOSAL (2026-07-29) recommending it sit adjacent to BUNDLE-020, whose
  # OQ-5 (shared spine or separate) it directly shares. Reorder to reprioritize.
  - id: BUNDLE-031
    title: Release Currency Versioning Machinery
  # Re-listed 2026-08-14 per founder ruling (relayed via team-lead): citation by a
  # directive that has since reached `done` does NOT count as homed. BUNDLE-004 is
  # cited by DIR-004, DIR-005 and DIR-009 — all three `done` — so its delivered
  # scope (SPEC-013/014/015/016/017) is spent and nothing active carries
  # pack-manifest surface design. Position is APPEND ORDER, not a priority claim;
  # reorder to reprioritize. Visible dependent: ISSUE-121 "Pack manifest has no
  # stack-policy surface, blocking doctor's runtime check" — BUNDLE-003 REQ-024
  # (doctor warning when the installed runtime deviates from pack-declared stack
  # policy) is unimplementable until this bundle designs the manifest field to
  # declare that policy; SPEC-070 carved REQ-024 out for this reason. ISSUE-121 is
  # deliberately cited by NO directive and rides this entry instead. The same
  # ruling applies to BUNDLE-005 (`ready`) and BUNDLE-008 (`exploring`), which are
  # in the identical state; the founder extended it to both on 2026-08-14 and they
  # are re-listed immediately below.
  - id: BUNDLE-004
    title: Pack Manifest and Authoring (design owner of the pack-manifest surface; ISSUE-121 rides here)
  # Re-listed 2026-08-14, same founder ruling extended (relayed via team-lead):
  # citation by a directive that has since reached `done` does not count as homed.
  # BUNDLE-005 is cited only by DIR-004 and DIR-009, both `done`. Position is
  # APPEND ORDER, not a priority claim; reorder to reprioritize. Re-listed
  # PROACTIVELY per the ruling, NOT because anything is currently blocked on it —
  # this is the deliberate difference from BUNDLE-004, which came back carrying a
  # live blocked requirement (ISSUE-121). Caveat recorded so a reader does not
  # over-read this entry: BUNDLE-005's validation substance ships today as
  # `pkg/packval`, and its one live residual defect (ISSUE-092, pack-test phase-3
  # fixtures cannot fail) is ALREADY carried by DIR-024 and DIR-032 — so this
  # entry marks unhomed intent, not uncarried work.
  #
  # BUNDLE-008 (Gate Diff Scoping) was re-listed here alongside BUNDLE-005 under
  # the same ruling on 2026-08-14, then REMOVED again the same day once it
  # reached `maturity: delivered` (its substance shipped via SPEC-018, now
  # status: implemented and back-annotated with supports: refs) — a delivered
  # bundle is not an open backlog slot.
  - id: BUNDLE-005
    title: Pack Validation — How an Agent Verifies Its Pack Output Is Correct

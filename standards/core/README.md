# core standards pack

Backstop's own enforcement — validates that backstop artifacts (schemas, specs, plans, bundles, etc.) conform to their declared schemas and structural rules.

This is the pack that backstop uses to police itself.

## Contents

- `policies/` — OPA policies for artifact validation
- `testdata/` — Valid and invalid fixtures for policy testing

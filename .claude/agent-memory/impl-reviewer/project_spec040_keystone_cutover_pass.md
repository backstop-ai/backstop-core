---
name: spec040-keystone-cutover-pass
description: SPEC-040 (BUNDLE-011 Seed 2) keystone toolchain-pack cutover PASSED review — real un-stubbed golden harness, both transitional seams wired, loud no-pack state live in dogfood gate
metadata:
  type: project
---

SPEC-040 (BUNDLE-011 Seed 2, branch bundle/011-codecheck-cutover @ 7c4bb0a, 31 claims) PASSED impl review. The keystone: deletes realCodeChecker/pkg/check.Run as gate Step 2 + builtinToolchain (go+ts baked stacks); lint/build/test run only via dispatchPackEngines over <lang>-toolchain packs.

What made it clean (worth repeating):
- **Golden-equivalence harness NOT the recurring pack-provisioning gap** (the thing I most feared given [[project_pack_provisioning_integration_gap]]). It is genuinely un-stubbed: dispatchPackEnginesFn==nil guarded, real on-disk go-toolchain pack via ParseManifestFile, real runFindingsEngine convert→SARIF→ParsePackFindings, convert script REALLY executed (runConvertScriptDirect shells the real *-to-sarif.sh via /bin/sh — only the sandbox-exec WRAPPER is bypassed for cross-platform). Tool OUTPUT is captured-bytes fixtures (required — can't assert equivalence with two live-tool runs). convertStdin spy proves the production convert pipe was reached; source guard bans exec.Command/os/exec in golden_equivalence.go. Golden fixture has 8 real violations, not a trivial []. Lesson: "stubbed SandboxedRunStdout wrapper + fixture tool bytes + REAL dispatcher" is the CORRECT shape for a deterministic golden proof — distinguish it from the SPEC-035/037 gap (testdata fed to a STUBBED dispatcher).
- **Both transitional seams wired + tested behaviorally:** CLM-028 coverage-not-orphaned (newSharedTestRunner survives, buildCoverageStep still fed — source+step-list assert); CLM-029 build-pass ProjectWide preserved on engine path keyed off TOOL-NEUTRAL engine.GateTypeBuild (not a "go build" sniff), set on go-build binding in DefaultRegistry. Real test proves a build break in a NON-diff-scope file (pkg/widget/gadget) carries ProjectWide=true through real dispatchPackEngines; boundary test proves lint does NOT. pkg/gate/scope.go:194 consumer (if violation.ProjectWide → skip filter) is pre-existing/intact → end-to-end real.
- **REQ-006 loud no-pack state LIVE in the dogfood gate:** ./bin/backstop gate shows `toolchain_enforcement ⚠ warning (enforcement not configured (0 toolchain packs))`, 5 passed/0 failed/3 warned, exit PASS. Distinct from green (CLM-021 compares rendered reports).
- **Retain-reduced internally consistent:** resolveToolchain reduced to declared-only (no builtin overlay), commandExecutor/buildExecutorsForConfigErr retained; code check subcommand survives. CLM-030/031 exercise the REAL retained path via DeclaredToolchainExecutorsForTest→buildExecutorsForConfigErr (not a shim). TestCutover_NoDeletedSymbolHasSurvivingCaller present+passes.
- realCodeChecker/builtinToolchain/checkViolationsToGate: ZERO non-test source matches. StepCodeCheck residual in cmd/backstop non-test = a COMMENT at gate.go:589 only; pkg/gate StepCodeCheck const/StepCodeCheckScopedFunc helpers are pkg/gate API, out of SPEC-040 deletion scope (cutover deletes the WIRING, not the pkg/gate helper).
- No Seed-3/4 bleed: step_coverage.go untouched; exempt_from_scope_filter only in a comment (not built); no coverage-records channel.
- Coverage: cmd/backstop 90.2 / pkg/check 92.6 / pkg/gate 90.1 / pkg/pack/engine 95.7 (threshold 80).

NON-BLOCKING artifact-hygiene note flagged to coordinator: Seed 2 deleting realCodeChecker/builtinToolchain forced removal/rewrite of TWO Seed-1 (SPEC-039) MANDATED tests — CLM-005 TestCodeCheck_NonGoFile_GateResultUnchangedAfterCatchAllRemoval (used realCodeChecker, deleted) and CLM-020 TestCodeCheck_MissingToolchain_StillConfigErrorAfterManifestBranchRemoval (registry-path config error gone with builtinToolchain → rewritten to ..._NoDeclaredToolchainIsCleanNotConfigError, asserting the NEW clean-exit-0 behavior). Both are legitimate align-predating-artifacts consequences, but SPEC-039's claims now reference tests that no longer exist — the align should update SPEC-039's claim text too.

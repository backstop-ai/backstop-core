---
name: spec042-coverage-producer-pass
description: SPEC-042 (BUNDLE-011 Seed 4) coverage PRODUCER passed review — real un-stubbed installed-pack E2E (NOT the provisioning gap), distinct non-SARIF records channel, single reconciled CoverageRecord, no Seed-3 bleed
metadata:
  type: project
---

SPEC-042 (BUNDLE-011 Seed 4, branch bundle/011-codecheck-cutover @ 2751832, 25 claims) PASSED impl review. The coverage PRODUCER: a SECOND normalized output type (coverage-records, non-SARIF), the canonical check.CoverageRecord, and the go-toolchain coverage engine.

What made it clean:
- **Real un-stubbed installed-pack E2E (the recurring gap — clean):** dispatch_coverage_e2e_test.go mirrors dispatch_astgrep_e2e_test.go. dispatchPackEnginesFn==nil guarded; the real coverage-to-records.sh on disk is executed via stubSandboxedRunStdout→runConvertScriptDirect (/bin/sh the REAL script — NOT a canned-records stub); convertStdin spy asserts the raw Go profile reached the convert pipe; real per-file aggregation asserted (passing.go 9/10=90%, failing.go 4/10=40%, iface.go total==0 N/A). A stubbed convert would fail the spy/aggregation assertions. The fixtureRunner only supplies the `go test -coverprofile` STDOUT (the profile) — required, since the real `go test` can't run deterministically; the CONVERT is real. No exec.Command/os/exec parallel path in any coverage file.
- **Non-SARIF channel is genuinely distinct:** dispatchPackCoverage→runCoverageEngine→check.ParsePackCoverage→[]CoverageRecord, NEVER ParsePackFindings (line-level confirmed; ParsePackFindings only in the separate runFindingsEngine). Routes SOLELY on binding.GateType==engine.GateTypeCoverage (no command/pack/engine-name sniff). ParsePackCoverage rejects a leading-'{' SARIF/object doc (anti-tunneling, CLM-007) — double-guarded at parser + dispatch. Tunneling fixture is a real SARIF 2.1.0 doc with coverage in result.properties. DisallowUnknownFields.
- **Canonical CoverageRecord{Path,Covered,Total,Measured,Excluded,Metric}:** reflect-based structural tests prove raw int counts, NO float/Pct field, NO Package noun. Total==0 preserved faithfully (no synth 0%); empty-metric-on-measured fail-louds. SINGLE type (reflect proves dispatchPackCoverage out-type == consumer in-type == check.CoverageRecord, no Pct). Convert script is real POSIX awk per-file statement aggregation, emits measured:true incl PASSING files (CLM-008 census non-vacuous).
- **go-coverage declared via pack.yml engines: block (declared substrate, NOT DefaultRegistry)** — thin-executor honored; pack carries all Go-coverage knowledge, dispatch bakes none (source guard bans -coverprofile/mode: atomic in pack_gate.go).
- **6 nil->m reconciliations SUBSTANTIVE not gutted:** 4 sites wrap dispatch in excludeDedicatedStepRules(...) = exact production behavior (coverage engine now partitioned out of SARIF dispatch, routes to records channel); golden-equiv still asserts its 8-violation reproduction. 2 pack_separation sites nil->m so engine-classification resolves PACK-declared engines (baked-registry-only nil would miss go-coverage) — strengthened, and itself evidence the engine is pack-declared not baked.
- **No Seed-3 bleed:** step_coverage.go UNTOUCHED (old baked analyzer, Seed 3 owns); exempt_from_scope_filter only in a comment; the threshold/verdict consumer (minimalCoverageConsumer) is TEST-LOCAL only (no prod consumer/catalog leak — gate step fenced to SPEC-041).
- **Bonus:** the commit also reconciled SPEC-039's claim test names (CLM-005/CLM-020 → the renamed Seed-2 tests) — closing the artifact-hygiene flag I raised in the SPEC-040 review. See [[project_spec040_keystone_cutover_pass]].

Coverage: pkg/check 92.8 / cmd/backstop 90.0 / pkg/pack/engine 95.7 (threshold 80). Gate coverage_threshold red is VERIFIED-inherited (old whole-repo aggregate on pre-existing pkg/config 87.9% + pkg/pack/distribution 89.9% — packages SPEC-042 never touched; the exact aggregate anti-pattern Seed 3 replaces with per-file).

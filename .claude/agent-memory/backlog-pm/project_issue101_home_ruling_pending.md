---
name: issue101-home-ruling-pending
description: ISSUE-101 (go-distribution pack) is cited by no directive; its unruled home now gates a growing family of child artifacts — do not slot any of them
metadata:
  type: project
---

**ISSUE-101 "Go Distribution Pack — Productize the Release Trinity" is homeless** —
open, `PLAN-ISSUE-101` `completed`, and cited by **no directive**. Escalated
2026-07-29T01:35Z; as of 13:59Z the same pending ruling gates **four** artifacts:

1. the gate-verdict-honesty cluster (see [[gate-verdict-honesty-cluster]])
2. ISSUE-101 itself
3. **BUNDLE-031 "Release Currency Versioning Machinery"** — its OQ-3 names the
   `go-distribution` pack as a candidate home for its own machinery
4. **ISSUE-111 "Backstop Core Adopts Go Distribution Pack"** — the durable-adoption
   residual PLAN-ISSUE-101 deliberately deferred (proved consumption in discarded
   worktrees only)

They all share the placeholder name `DIR-028`.

**The fork, so you don't re-derive it:** **DIR-001 "Release Workflow"** (its Notes
carry the founder's *go-distribution pack framing* bullet, and it owns the release
trinity the pack's rules gate — ISSUE-101's own References point at its
"Release-pipeline follow-on routings" section) vs **DIR-027 "Pack Fleet Publication
& Migration"** (thread 3 owns which consumer lock points at which coordinate). My
standing recommendation is **DIR-001** unless Brandon creates a go-distribution
directive, in which case the whole family moves there together.

**Why:** PLAN-ISSUE-101's TASK-016 filed ISSUE-109/110/111 as closure hand-offs and
routed a fourth candidate into BUNDLE-031 rather than filing it — the lane is
actively spawning children off an unruled parent, so each new one re-poses the same
question.

**How to apply:** when a `go-distribution` / release-currency artifact arrives,
**do not slot it**, even if DIR-001 looks like a clean fit — slotting a child answers
the parent's home by filing convention. Escalate, restate the claimant count, and ask
for the ONE ruling. Expect concurrent PM passes to reach this independently (the
ISSUE-109 and ISSUE-111 triages landed the same conclusion in the same minute) — see
[[concurrent-pm-triage-races]]. Re-check `grep -rl ISSUE-101 directives/` before
citing this; the moment a directive cites it, this memory is dead.

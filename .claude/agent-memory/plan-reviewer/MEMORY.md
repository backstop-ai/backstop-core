- [Phase-3 fixture polarity inverted](project_phase3_fixture_polarity_inverted.md) — on packval's FINDINGS seam `Passed=false`+nil err is a POSITIVE fixture's success (silent); a NEGATIVE fixture REDS on it — plans state the inverse
- [Sibling precedent cited, not read](project_sibling_precedent_cited_not_read.md) — "per the ISSUE-NNN precedent this is out of scope" often INVERTS it; the sibling states the same doctrine and fixes both owners in-lane, with a filing task
- [Stale sibling-scaffold evidence](project_stale_sibling_scaffold_evidence.md) — "sibling plan N is an EMPTY SCAFFOLD" expires in MINUTES on a parallel-authoring night; re-extract every sibling's phases + `files:` yourself (untracked ≠ empty)
- [Reviving dead dispatch adds a tool dep](project_reviving_dead_dispatch_adds_tool_dep.md) — widening an eligibility guard gives PRE-EXISTING unguarded tests a hard ast-grep/semgrep/jq requirement; count subprocesses before vs after
- [Data-driven RED's uniform quantifier](project_datadriven_red_uniform_quantifier.md) — "PRE-FIX: BOTH/ALL rows fail" is per-subtest; the row the planner flags as passing "for the wrong reason" is usually already GREEN under the plan's own literal predicate
- [Lane enumeration misses pending tasks](project_lane_enumeration_misses_pending_tasks.md) — contention sections derive lanes from `git status` (LIVE edits) and miss in-progress plans whose `files:` include yours but whose touching task is still pending; grep `plans/*.yml` instead
- [Prefix classifier blind to suffix](project_prefix_classifier_blind_to_suffix.md) — a rename plan citing its OWN prefix-keyed check as the safety net goes GREEN on a half-migrated id; run the plan's algorithm on the wrong variant by hand
- [Undefined claim roster](project_undefined_claim_roster.md) — issue-sourced plans must define CLM-NNN in notes; the validator never checks it, so two review rounds can miss claims referenced-but-undefined
- [packval real-execution premises](project_packval_real_execution_premises.md) — "pack X passes phase 3 once dispatch is live" is 3-ways false: executor skips `convert:`, no Pattern field, baked semgrep rule-ID check; run the pack's engine yourself
- [Three measurement layers, not two](project_sarif_suppressions_measurement_layer.md) — raw SARIF ⊃ parseSarif-active ⊃ gate-printed; `@waiver:` on the line ABOVE a predicted row makes its "disappears" vacuous and breaks before/after totals
- [Stated convention vs byte arithmetic](project_stated_convention_vs_byte_arithmetic.md) — divide quoted bytes/files: a plan's "absolute-path, unpruned" label is often relative+unpruned; the delta was pruning, not path form
- [Scan-boundary count mismatch](project_scan_boundary_count_mismatch.md) — a plan's "N files under dir" label often excludes testdata while the prescribed walk includes it; run both finds — the boundary IS the claim
- [Falsification revert on shared tree](project_falsification_revert_shared_tree.md) — proving teeth by editing a tracked file + `git checkout --` wipes a concurrent lane; demand confirmed-clean target or an untracked probe
- [E2E fixture already loud at HEAD](project_e2e_fixture_already_loud_at_head.md) — "observe the silent pass at HEAD" tasks must name the fixture field (nil/non-nil Provision etc.) that makes HEAD silent; an adjacent guard often refuses first → green-at-HEAD acceptance test
- [Inert decoy fixtures](project_inert_decoy_fixtures_vacuous.md) — "sees X and nothing else" guards are vacuous when decoys are content-less: the extractor skips unparseable input silently, so the widened-filter mutation is invisible
- [Short-circuit-dependent guard](project_shortcircuit_dependent_guard.md) — "the existing mandated test already covers this" is often FALSE: an earlier predicate branch (`+dirty`) short-circuits, so the guard is green at HEAD with the defect live; measure it
- [Fail-loud consumption-path trace](project_fail_loud_consumption_path.md) — for ISSUE-003/005/008-family config-error plans, trace every executor-construction path, not just the guard site
- [Retirement-claim scope](project_retirement_claim_scope.md) — "retire/replace X" claims must scope the file where X is DEFINED, not just the new call site; check test_command covers added test packages
- [Shared-helper collapse blast radius](project_shared_helper_collapse_blast_radius.md) — a branch edit inside a shared helper/seam changes EVERY production caller; grep call sites, plans name only the one the issue mentions
- [Dispatch consumer edges](project_dispatch_consumer_edges.md) — consumer-impl tasks must depends_on the impl of every symbol they import, not just their own test; validator misses this
- [Field-removal fixture scope](project_field_removal_fixture_scope.md) — flag-day field removal must scope EXISTING test files + testdata fixtures that set the old field, not just production code
- [Retired-feeder test collateral](project_retired_feeder_test_collateral.md) — retiring a feeder/seam (mergePackRules, ExtraSemgrepConfigs, a `Fn` override) breaks existing tests that call/monkeypatch it; scope them
- [Symmetric field-removal claims](project_symmetric_field_removal_claims.md) — paired field removals (manifestDir + Options.ManifestDir) need symmetric claim attribution; verify the collateral test-file surface empirically equals the scoped files
- [Signature-change package fanout](project_signature_change_package_fanout.md) — a Go func signature change radiates to every caller in the package + `var Fn=func` seams + prod call sites; grep before trusting plan scope
- [Whole-file delete shared types](project_whole_file_delete_shared_types.md) — "delete file X in full" must check X doesn't DEFINE types/helpers (ConfigError) used by surviving live code
- [Whole-file delete mandated-test readsource](project_whole_file_delete_mandated_test_readsource.md) — deleting a file breaks SURVIVING-spec mandated tests that readCheckSource() it — runtime t.Fatalf, not compile error
- [Deletion file-premise audit](project_deletion_file_premise_audit.md) — "all N files reference deleted symbols" — grep each; some couple only to SURVIVING symbols, blanket-delete drops coverage
- [Ast-grep pack convert-script scope](project_astgrep_pack_convert_script_scope.md) — every pack dispatched through real ast-grep needs its own ast-grep/to-sarif.sh in task scope; dispatch hard-errors "broken pack: missing convert script" if absent
- [Cross-seed pack location + convert](project_cross_seed_pack_location_convert.md) — seed plans adding to a prior seed's shared pack must match the owner seed's actual pack path/layout; every real-ast-grep/grep pack needs its own ast-grep/grep to-sarif.sh at pack root (TDD packs too, not just the E2E pack)
- [Coverage-rewrite predating-spec drift](project_coverage_rewrite_predating_spec_drift.md) — deleting/rewriting baked machinery strands EARLIER specs' mandated tests (SPEC-041 vs SPEC-010 coverage claims); plan needs a spec-update task
- [Repurposed-test claim-text drift](project_repurposed_test_claim_text_drift.md) — keeping a mandated test's NAME while flipping its body passes the gate existence check but strands the claim's TEXT if it still asserts the old behavior; needs a spec-author task
- [Reconciliation-swap enable wiring](project_reconciliation_swap_enable_wiring.md) — reconciliation passes need enable+feed wiring at cmd/backstop construction site, not just the pkg/gate swap; pkg/gate e2e cant prove the CLI enables it
- [Consumer-scope E2E harness fixtures](project_consumer_scope_e2e_harness_fixture.md) — consumer-level status-scoping blast radius includes non-_test.go E2E harness sources embedding draft specs; positive assert=RED, negative assert=VACUOUS
- [Shared-cache seam wiring](project_shared_cache_seam_wiring.md) — cmd/backstop per-gate state must thread the dispatchPackEnginesFn SEAM (test path, ~15 spies), not a direct dispatchPackEngines call; coverage path is direct
- [Gate source-string assertions](project_gate_source_string_assertions.md) — cmd/backstop tests string-match exact call forms in gate.go source (e.g. coverageRecordsProducer(packs, projectRoot)); rewiring a pinned call site reds them invisibly
- [Nil-seam default needs reachable data](project_nil_seam_default_needs_reachable_data.md) — "nil seam = real path" fails if the lib can't reach the gate's args; waiver.Adjudicate is LINE-windowed
- [Self-pack absence-claim tests](project_selfpack_absence_claim_tests.md) — backstop/self is external+gitignored (hard-fail test reds CI); most rule families are path-scoped away from new packages
- [Verified enumeration + do-not-re-derive](project_verified_enumeration_do_not_rederive.md) — re-sweep any plan that publishes a "verified, do NOT re-derive" file:line list; omissions become DIRECTED misses
- [Pre-existing RED claim needs amendment node](project_preexisting_red_claim_needs_amendment_node.md) — claim red pre-work = founder blocker; demand an amendment node OR proof the spec already moved, then re-measure the amended predicate
- [Captured-fixture source must exist](project_captured_fixture_source_must_exist.md) — verify each named fixture source exists AND has the asserted property; 'real X that never mentions Y' is often unobtainable and vacuous
- [Contract signature-block drift](project_contract_signature_block_drift.md) — a signature/field change strands `signature:` in EVERY implemented spec contracts block naming it; grep specs/ for the symbol, not just the cited spec
- [De-baking sweeps ALL specs](project_debake_literal_sweeps_all_specs.md) — removing a baked literal? grep specs/ corpus-wide for the LITERAL and for the SYMBOL; planners grep only the spec they think owns it
- [New guard predicate: measure existing fixtures](project_new_guard_predicate_measure_existing_fixtures.md) — run the real engine over EVERY pre-existing fixture and evaluate the proposed guard on the measured counts; precision arguments miss legit inputs
- [Injected-param call sites unfalsified](project_injected_param_callsite_unfalsified.md) — "every call site populates X" is not proven by walk/merge tests; demand a real-command test or it's a blocker
- [Mandated-name premise needs spec status](project_mandated_name_premise_needs_spec_status.md) — "renaming this test reds status_drift" is false for `replaced` (excluded) and `draft` (not due) specs; only `implemented` enforces
- [Traceability step can't report off-corpus](project_traceability_step_cannot_report_offcorpus.md) — refs whose citing path isn't in the NON-RECURSIVE status walk are dropped; "traceability reports no planted file" is a vacuous falsifier
- [Sweep-axis definition drift](project_sweep_axis_definition_drift.md) — run each mandated grep LITERALLY; plans carry incompatible defs of the same named sweep and overclaim new "axes"; also re-check LANE STATUS vs live git status
- [Sibling-lane exclusivity fence](project_sibling_lane_exclusivity_fence.md) — same-day UNCOMMITTED sibling plans often fence scope in ONE plan's notes naming the other issue; grep the sibling's notes for your issue ID + "not in scope"
- [Base registry overrides fixture binding](project_base_registry_binding_overrides_fixture.md) — packval `Engine: "semgrep"` resolves to the BASE binding (real semgrep, non-nil Provision); a fixture command needs a pack-level `Engines:` override or the red is misattributed
- [Producer-swap argv keeps the subcommand](project_producer_swap_argv_subcommand.md) — a `producer:` replacing the TOOL gets cmdArgs incl. `test`/`build`; `go test "$@"` = `go test test ./...` → non-zero on a GREEN tree; fixtureRunner ignores args so no unit test catches it
- [Deleted symbol named in kept comment](project_deleted_symbol_named_in_kept_comment.md) — delete-a-symbol + "zero grep hits" evidence collides with "KEEP this comment" when the comment names it; grep the kept comments
- [Gated sub-check unreachable pin](project_gated_subcheck_unreachable_pin.md) — "pin edge-case I through the EXPORTED validator" goes vacuous when the sub-check sits behind an earlier guard (`if filenameOK`); grep the call site
- [Prescribed pattern byte shape](project_prescribed_pattern_byte_shape.md) — a plan prescribing an awk/regex over real tool output asserts BYTES; `od -c` the line (`FAIL\tpkg [build failed]` is one tab + a SPACE), and diff every occurrence within the plan
- [Same bytes, two mandated tests](project_same_bytes_two_mandated_tests.md) — one fixture feeding two mandated tests often hides a contradiction; pipe the real capture through the committed converter to prove red-first
- [Completeness-claimed comment set](project_completeness_claimed_comment_set.md) — "the corrections are a set of N, complete this time" is still wrong; sweep yourself over a TWO-LINE window and include files in no task's scope
- [Uncommitted lane rows move pinned counts](project_uncommitted_lane_rows_move_pinned_counts.md) — a re-measured finding count that disagrees with the plan is usually sibling-lane UNCOMMITTED edits; map each extra row to a `git diff -U0` hunk before flagging
- ["Registry-derived" is per-test, not per-file](project_registry_derived_premise_per_test.md) — a file can mix `range registry()` tests with HARDCODED-slice tests; check each named "unaffected" test, then sweep the plan for the premise restated in task bodies
- [Prior lane planted a fence](project_prior_lane_planted_fence.md) — grep the plan's OWN issue id across pkg/cmd *.go: a completed sibling often planted an assertion naming your future change; read fixture INPUTS, not just the assertion
- [Unconfigured artifact root](project_unconfigured_root_exclusion_vacuous.md) — backstop.yml declares no artifact_root, so ResolveRoot returns the PROJECT root; "exclude paths under the artifact root" drops EVERYTHING — demand Root.Dir(kind)
- [Census through the real parser](project_census_through_real_parser.md) — reproduce a plan's token census by running ParseToken, not grep: tokens inside Go string literals are MALFORMED (trailing `"})` breaks the expiry), not unbound
- [New advisory violation clears TWO filters](project_new_advisory_violation_two_filters.md) — a new warning `gate.Violation` is either dropped by `FilterViolations` (empty File, !ProjectWide) or REDs the baseline step (hardcoded "fail" on any NewViolation); `blocksVerdict` answers neither
- [Source-text anchor first-match](project_source_text_anchor_first_match.md) — "isolate symbol X's doc comment" anchors hit the COMMENT line (Go doc convention), selecting the PRECEDING field's block: negative assert vacuous, positive assert confusing
- [Inert-engine premise, wrong reason](project_inert_engine_premise_wrong_reason.md) — "no live victim" conclusions are usually right for the wrong reason; a rule DOES bind the engine, fixture absence is what makes it inert (packval phase3 dispatches only inside fixture loops)
- [Wrapped literal defeats substring guard](project_wrapped_literal_defeats_substring_guard.md) — a `must not contain "always pass"` guard is FALSE on emitted bytes when the phrase wraps across a line + `#`; scaffold and run the real search per arm
- [Single-authority refactor unfalsifiable](project_single_authority_refactor_unfalsifiable.md) — "route duplicate X through one shared predicate" claims: the agreement test is green before AND after, so the refactor task can be deleted and all tests still pass
- [Cited fixture precedent reads other files](project_cited_fixture_precedent_reads_other_file.md) — "follow the shape in testdata/X" goes vacuous when the NEW check reads rule files while X's check read only pack.yml; derive the file set from the check's own reads
- [Quantifier pinned by cardinality one](project_quantifier_pinned_by_cardinality_one.md) — "ANY half is already pinned by pack X" is vacuous when X's quantified set has size 1; tabulate the dimension per unit and check the real case's cardinality
- [Doctored fixture pre-empted by validation](project_doctored_fixture_preempted_by_validation.md) — "remove the fixtures: block to force an empty derivation" hits a parse error FIRST; the subtest still greens on the wrong branch
- [Diff-scoped CI count confound](project_diffscoped_ci_count_confound.md) — "expect fewer than the failed run's 62 violations" is trivially true: CI gates DIFF-SCOPED, so a 2-file push shrinks the count regardless of the fix
- [Enclosing-param guard is still spelling](project_ast_guard_enclosing_param_still_spelling.md) — a trailing decoy param of the same type, or a local re-bind, greens the "provenance-aware" AST rewrite; prototype it
- [go-test SARIF: one violation per test](project_gotest_sarif_one_violation_per_test.md) — converter keeps only the FIRST position per `--- FAIL:`, and go-test is scope-filter EXEMPT; both break plan CI criteria
- [Seatbelt device-node writes are emergent](project_seatbelt_devnode_writes_emergent.md) — macOS permits /dev/null AND /dev/zero writes under a blanket `(deny file-write*)`; a darwin /dev/null carve-out is a behavioural no-op + regression lock, never a red→green
- [Verification -run hits wrong package](project_verification_run_wrong_package_vacuous.md) — `go test <pkg> -run Name` for a test in ANOTHER package exits 0 "[no tests to run]"; map test names to owning package
- [Self-closing plan: order + post-mutation verify](project_self_closing_plan_order_and_post_mutation_verify.md) — delivered_by needs the PLAN completed FIRST, 3 issue fields, and a verification task AFTER the artifact mutations (pre-close validate is vacuous)
- [GHA steps-context null coercion](project_gha_steps_context_null_coercion.md) — `steps.<bad-id>.conclusion != 'skipped'` is TRUE (null→0 vs NaN): guard goes INERT, not skip; plans state the inverse
- [Retracted claim survives in shipping text](project_retracted_claim_survives_in_shipping_text.md) — a retraction lands in notes/sharp-edges but the copy inside a task's PRESCRIBED yaml comment / error string ships anyway; grep the literal
- [Monotone counter ships stale](project_monotone_counter_ships_stale.md) — "N consecutive failing runs" measured at plan time is wrong by implement time; re-query each round, demand ≥/date-stamped phrasing
- [Cited issue's self-retracted evidence](project_cited_issue_self_retracted_evidence.md) — a plan quoting `ISSUE-NNN lines X-Y` often lands on Evidence bullets the SAME issue's Root cause section retracts
- [Matrix-test subject provenance](project_matrix_test_subject_provenance.md) — "runs the CONDITION under N shells" without naming WHERE the condition comes from: both hardcodings break the red→green trajectory
- [Rewritten body, absence predicate](project_rewritten_body_absence_predicate.md) — a "prove the BODY changed" close-out check must be PRESENCE-only; arrange code is legitimately identical, so an absence grep reds a correct fix
- [Import premise from grep](project_import_premise_grep_matches_forbidden_lists.md) — "X imports pkg/Y" from `grep -rl` is often FALSE: the hit is a forbidden-import string literal in a leaf pkg's own test; run the AST predicate
- [Source issue fences out delivered claim](project_source_issue_fences_out_delivered_claim.md) — a scope-widening plan leaves the issue's own "that's ISSUE-NNN's territory" section un-retracted; enumerate fence sites
- [RED signature matches buggy impl](project_red_signature_matches_buggy_impl.md) — a restructured guard's stated "A and B both fail" pre-fix set is often satisfied only by the un-refactored version; derive the set yourself
- [Narrow grep, false missing citation](project_narrow_grep_false_missing_citation.md) — a cited symbol returning zero hits is usually MY grep (`func ` prefix, assumed json tag spelling), not a bad citation; re-grep bare before flagging

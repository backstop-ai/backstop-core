---
name: spec046-review1
description: SPEC-046 (BUNDLE-012 Seed 3, retire language toolchain bridge) — review #1 FAIL, review #2 PASS (ready to plan)
metadata:
  type: project
---

SPEC-046 (retire `language:` bridge + field + rehome traceability stack label; owns bundle REQ-004/REQ-005 + SQ-1).

**Review #2 = PASS (ready to plan).** Validator PASS (prior YAML parse error gone). All three review-#1 blockers + three nits closed and verified vs live code 2026-06-29:
- B1 (existing-test coupling): REQ-001/002/003 now NAME every behavior-coupled existing test + an "Existing tests to delete or rewrite" table; new CLM-023/024/025 with SOURCE GUARDS (`TestGate_NoTestReferencesDeletedBridgeSymbols`, `TestConfig_NoTestAssertsConfigLanguage`) that make a green-keeping shim impossible. Incidental `Config{Language:}` fixtures left to the generic sweep + CLM-011 compile fence — correct granularity per [[existing-test-coupling]].
- B2 (SQ-1 signal conflation): single authoritative signal = empty declared toolchain-pack-NAME set; HasSourceGlobs demoted to corroborating, "MUST consume" mandate dropped; divergence case (toolchain pack w/ no classification globs) documented in REQ-004 + CLM-018 + a sharp edge.
- B3 (wrapTraceabilityStep threading): new contract entry `wrapTraceabilityStep(cfg, dim, stepName, stack, delegate)` + corrected call graph buildGateSteps→wrapTraceabilityStep→deriveCapabilityState w/ all 3 call sites (~L653-655). Matches live signature (insert `stack` before `delegate`).
- Nits: mergeSourceClassifier param now `packs` (matches SPEC-043 canonical); prose now "SPEC-043 repoints, 046 fences"; unsubstantiated "SPEC-043 deferred SQ-1" replaced with "owned per Seed 3 assignment".

**Residual nit (non-blocking):** REQ summary table row (line ~464) still reads "consume SPEC-043's SourceClassifier (HasSourceGlobs)" without the corroborating-only demotion — stale vs the corrected REQ-004 body. Cosmetic.

**Verified GOOD (carried from #1):** backstop.yml declares backstop/go-toolchain:local (CLM-007 premise holds); 5 production cfg.Language readers enumerated; look-alikes pack.Manifest.Language + check.Options.Language fenced; B2/CLM-022 consistent with SPEC-043's `mergeSourceClassifier(packs)` single-arg reconciled contract; named existing tests all exist with claimed content (gate_bridge_load_test.go, gate_bridge_agnostic_test.go, cutover_noregress_test.go, gate_no_toolchain_pack_test.go 2-arg, gate_capability_test.go NonGo tests L17/71, config_test.go:26).

**Review #3 (targeted, collateral-reader re-extension) = FAIL.** PLAN review caught omitted config.Config.Language readers (registry_test.go, out of orig guard scope). Author extended REQ-003/CLM-025/table/test_command to add gate_wiring_test.go (behavioral REWRITE), gate_capability_rekey_test.go, gate_capability_contracts_rekey_test.go, cutover_deletion_test.go, pkg/check/registry_test.go; guard scope + test_command now include ./pkg/check/; check.Options.Language fenced. GOOD + verified: guard scope is now EXHAUSTIVE at package level — repo-wide every config.Config.Language site lives in {cmd/backstop, pkg/config, pkg/gate, pkg/check}, no registry_test.go-style out-of-scope escape remains; registry_test.go bridge (L85 config.Config{Language}, L154/203/233/258 cfg.Language→Options.Language, L71/75 literal Options.Language surviving) described accurately.
BUT exhaustive sweep found TWO genuine config.Config.Language sites still OMITTED from the enumeration/table: `cmd/backstop/cutover_consistency_test.go` (L28+L65 `config.Config{Language:"go"}`) and `pkg/gate/traceability_polarity_step_test.go` (L15 `cfgDeclaring(language,…)` returns `config.Config{Language:language}`, feeds PolarityStepResult message/waive tests — REHOME-COUPLED: after stackLabel→cap.Stack the label no longer comes from cfg.Language, needs gate_wiring_test.go-style behavioral rewrite, not a token strip). Both in-scope of test_command so compile-break is caught, BUT CLM-025's guard ("no _test.go in those 4 pkgs reads config.Config.Language") is unsatisfiable by the enumerated rewrites alone → enumeration asserts a complete collateral list that is provably incomplete. Fix: add both to REQ-003 + table + CLM-025; flag traceability_polarity_step_test.go as behavioral.

**Review #4 (targeted, completeness-mechanism) = PASS — ready to plan.** Author resolved review #3 two ways: (1) reframed REQ-003/CLM-025 so the MECHANICAL completeness contract = source guard `TestConfig_NoTestAssertsConfigLanguage` (no _test.go in the 4 pkgs reads config.Config.Language) + CLM-011 compile fence + test_command over all 4 pkgs; the file list is explicitly ILLUSTRATIVE/known-readers, NOT the task boundary — removes the enumeration-vs-guard contradiction. (2) Added all 3 stragglers: cutover_consistency_test.go (strip), pkg/check/ts_executor_test.go (BRIDGE-FIX, L20/L30 cmdCfg.Language→Options.Language, accurately cited), traceability_polarity_step_test.go (rehome-coupled REWRITE, re-source from CapabilityState.Stack). Independently verified vs live code: exactly 10 config.Config.Language reader TEST files, ALL within {cmd/backstop, pkg/config, pkg/gate, pkg/check} — guard scope provably exhaustive, no out-of-scope escape; all 10 enumerated; check.Options.Language + pack.Manifest.Language (packval) fenced. SPEC-046 is done.
